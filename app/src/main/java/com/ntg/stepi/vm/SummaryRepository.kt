package com.ntg.stepi.vm

import com.ntg.stepi.models.res.SummaryRes

class Repository {



//    suspend fun getItems(page: Int, pageSize: Int): Result<List<SummaryRes>> {
//        delay(2000L)
//        val startingIndex = page * pageSize
//        return if(startingIndex + pageSize <= remoteDataSource.size) {
//            Result.success(
//                remoteDataSource.slice(startingIndex until startingIndex + pageSize)
//            )
//        } else Result.success(emptyList())
//    }
}